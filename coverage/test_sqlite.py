# A settings file to use for older versions of Django that don't include a
# default settings file for running the unit tests.
DATABASE_ENGINE = 'sqlite3'
TEST_DATABASE_NAME = 'testdb'
ROOT_URLCONF = 'foo'
ADMIN_MEDIA_PREFIX = 'foo'
